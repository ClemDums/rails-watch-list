<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie, collection: Movie.all %>
  <%= f.input :comment, label: 'Write your comment here', error: 'Your comment must be at least 6 caracters' %>
  <%= f.button :submit %>
<% end %>
